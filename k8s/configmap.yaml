apiVersion: v1
kind: ConfigMap

metadata:
  name: shop-config

data:
  SPRING_DATASOURCE_URL: jdbc:postgresql://postgres-service:5432/shop
  SPRING_DATASOURCE_USERNAME: postgres
  POSTGRES_DB: shop
  POSTGRES_USER: postgres